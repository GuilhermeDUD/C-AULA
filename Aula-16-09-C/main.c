#include <stdio.h>
#include <stdlib.h>
#include <time.h>

#define QUANTIDADE (40)
    // inicialize um vetor com os numeros aleatorios
    // busque nesse valor os elementos vizinhos com maior diferenca

int main (){
    int aleatorio(QUANTIDADE);

    //gerar numeros aleatorios
    srand(time(NULL));

    printf("Vetor:");
    for (i = 0; i < QUANTIDADE; i++)
    {
        aleatorio[1] = rand;
        printf("%d",&aleatorio[1]);
    }
    printf("\n")

    for (i = 0; i < QUANTIDADE; i++)
    {
        /* code */
    }
    




}